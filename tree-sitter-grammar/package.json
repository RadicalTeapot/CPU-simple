{
  "name": "tree-sitter-cpu-simple-asm",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for CPU-simple assembly language",
  "main": "bindings/node",
    "dependencies": {
    "tree-sitter-cli": "^0.26.5"
  },
  "scripts": {
    "build:generate": "tree-sitter generate",
    "build:parser": "mkdir -p build && tree-sitter build -o build/csasm.so",
    "build:copy": "mkdir -p ../nvim-plugin/queries/csasm && mkdir -p ../nvim-plugin/parser && cp queries/highlights.scm ../nvim-plugin/queries/csasm/ && cp build/csasm.so ../nvim-plugin/parser/",
    "build" : "npm run build:generate && npm run build:parser && npm run build:copy",
    "test": "tree-sitter test"
  }
}