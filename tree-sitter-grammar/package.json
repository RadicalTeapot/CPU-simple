{
  "name": "tree-sitter-cpu-simple-asm",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for CPU-simple assembly language",
  "main": "bindings/node",
    "dependencies": {
    "tree-sitter-cli": "^0.26.5"
  },
  "scripts": {
    "generate": "tree-sitter generate",
    "build": "mkdir -p build && tree-sitter build -o buid/csasm.so",
    "test": "tree-sitter test"
  }
}